{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "",          // skip install step bawaan Node
  "buildCommand": null,          // jangan jalankan build command apa pun
  "functions": {
    "api/**/*.py": { "runtime": "python3.12" }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/wsgi.py" }]
}
